# Use the official Nginx image as the base image
FROM nginx:latest

# Create a custom directory to store your HTML file
WORKDIR /usr/share/nginx/html

# Copy everything from the local directory into the c
COPY . .

# Expose port 80 to the outside world
EXPOSE 80

